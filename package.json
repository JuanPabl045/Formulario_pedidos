{
  "name": "formulario-pedidos",
  "version": "1.0.0",
  "description": "Formulario de pedidos para Molino Viejo - INTENTO FINAL",
  "scripts": {
    "start": "firebase emulators:start --only hosting",
    "dev": "firebase emulators:start --only hosting",
    "build:css": "cleancss -o public/css/site.min.css public/css/site.css || echo 'clean-css not found'",
    "minify:local": "node scripts/minify-css.js",
    "images:convert": "node scripts/convert-images.js",
    "optimize:images": "imagemin public/img/* --out-dir=public/img/optimized || echo 'imagemin not found'",
    "build": "npm run build:css && npm run optimize:images",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "sharp": "^0.34.5"
  }
}
